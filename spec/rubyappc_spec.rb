# frozen_string_literal: true

RSpec.describe Rubyappc do
  it "has a version number" do
    expect(Rubyappc::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
